#ifndef _PUTCHAR
#define _PUTCHAR

/**
 * _putchar - writes the character c to std
 * @c: parameter to compare
 * Return: 1 or 0
 */
int _isupper(int c);

int _isdigit(int c);

int mul(int a, int b);
#endif
